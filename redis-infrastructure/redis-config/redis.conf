# 메모리 설정
maxmemory 256mb
maxmemory-policy allkeys-lru

# 영속성 설정
appendonly yes
appendfilename "appendonly.aof"

# 네트워크 설정
bind 0.0.0.0
# 인증에 비밀번호 설정이 필요한 경우 protected 모드를 yes로 하고 비밀번호 설정
# protected-mode yes
port 6379

# 로깅 설정
loglevel notice
logfile ""
